# ═══════════════════════════════════════════════════════════════════════════════
# MCP Server 環境變數設定範本
# ═══════════════════════════════════════════════════════════════════════════════
# 複製此檔案為 .env 並填入實際值

# ─────────────────────────────────────────────────────────────────────────────
# API Key 認證 (JSON 陣列格式)
# ─────────────────────────────────────────────────────────────────────────────
# 格式說明：
# MCP_API_KEYS 為 JSON 陣列
# 每組包含：api_key (required), tools (optional), gmail_account (optional)
# tools: ["*"] 表示允許所有 tools，或指定 ["tool1", "tool2", ...]
# gmail_account: 綁定 Gmail 帳號（需在 gmail_accounts.json 設定）
MCP_API_KEYS=[{"api_key": "sk-YOUR-API-KEY-HERE", "tools": ["*"]}]

# ─────────────────────────────────────────────────────────────────────────────
# 伺服器與 Docker 容器身分設定
# ─────────────────────────────────────────────────────────────────────────────
MCP_HOST=0.0.0.0
MCP_PORT=8000

# Docker 容器內的使用者身分 (對應宿主機 UID/GID 以避免檔案權限問題)
UID=0
GID=0
USERNAME=your_username

# ─────────────────────────────────────────────────────────────────────────────
# Python 執行環境
# ─────────────────────────────────────────────────────────────────────────────
PYTHON_WORK_DIR=./workspace
MCP_SHELL_CWD=.

# ─────────────────────────────────────────────────────────────────────────────
# 執行限制
# ─────────────────────────────────────────────────────────────────────────────
MCP_EXEC_TIMEOUT=300
MCP_MAX_INPUT=1000000
MCP_MAX_OUTPUT=1000000

# ─────────────────────────────────────────────────────────────────────────────
# TMDB API (電影資料庫)
# ─────────────────────────────────────────────────────────────────────────────
# 申請網址: https://www.themoviedb.org/settings/api
TMDB_API_KEY=YOUR_TMDB_API_KEY

# ─────────────────────────────────────────────────────────────────────────────
# MySQL 資料庫
# ─────────────────────────────────────────────────────────────────────────────
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=your_mysql_user
MYSQL_PASSWORD=your_mysql_password
MYSQL_DATABASE=your_database_name
MYSQL_MAX_ROWS=10000

# ─────────────────────────────────────────────────────────────────────────────
# Gmail API (多帳號支援)
# ─────────────────────────────────────────────────────────────────────────────
# JSON 格式，可設定多組 Gmail 帳號
# 格式: {"email@gmail.com": {"client_id": "xxx", "client_secret": "xxx", "refresh_token": "xxx"}}
GMAIL_ACCOUNTS=

# ─────────────────────────────────────────────────────────────────────────────
# Playwright Web Automation
# ─────────────────────────────────────────────────────────────────────────────
# 是否以無頭模式啟動內建瀏覽器 (true/false)
PLAYWRIGHT_HEADLESS=true
PLAYWRIGHT_CDP_ENDPOINT=http://127.0.0.1:9222
PLAYWRIGHT_DEFAULT_TIMEOUT=30000

# ─────────────────────────────────────────────────────────────────────────────
# 遠端瀏覽器設定（WebSocket 反向連線）
# ─────────────────────────────────────────────────────────────────────────────
# 啟用遠端瀏覽器功能，讓 Windows 端的 Browser Agent 可以主動連入
REMOTE_BROWSER_ENABLED=true

# WebSocket Server Port（供 Browser Agent 連接）
REMOTE_BROWSER_PORT=8001

# 認證 Token（Browser Agent 需要提供相同的 Token 才能連接）
REMOTE_BROWSER_TOKEN=your-secret-token-here

# ─────────────────────────────────────────────────────────────────────────────
# Ollama Web API (選用)
# ─────────────────────────────────────────────────────────────────────────────
OLLAMA_API_KEY=YOUR_OLLAMA_API_KEY
OLLAMA_WEB_SEARCH_URL=https://your-api-proxy:8443/proxy/ollama/api/web_search
OLLAMA_WEB_FETCH_URL=https://your-api-proxy:8443/proxy/ollama/api/web_fetch
OLLAMA_WEB_TIMEOUT=30

# ─────────────────────────────────────────────────────────────────────────────
# Gemini API (選用)
# ─────────────────────────────────────────────────────────────────────────────
# JSON 陣列格式，可設定多組 Key
GEMINI_API_KEYS=[{"api_key": "YOUR_GEMINI_API_KEY"}]
GEMINI_PAY_KEY=
GEMINI_API_BASE_URL=https://generativelanguage.googleapis.com
GEMINI_PROXY_URL=http://your-internal-ip:47300/proxy/gemini

# ─────────────────────────────────────────────────────────────────────────────
# DeepSeek API (選用)
# ─────────────────────────────────────────────────────────────────────────────
DEEPSEEK_API_KEY=YOUR_DEEPSEEK_API_KEY

# ─────────────────────────────────────────────────────────────────────────────
# Ollama Proxy (選用)
# ─────────────────────────────────────────────────────────────────────────────
OLLAMA_PROXY_URL=http://your-internal-ip:47300/proxy/ollama
