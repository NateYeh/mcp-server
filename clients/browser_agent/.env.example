# ═══════════════════════════════════════════════════════════════════════════════
# Browser Agent 環境變數設定
# ═══════════════════════════════════════════════════════════════════════════════
# 說明：請複製此檔案為 .env 並填入實際設定值

# ─────────────────────────────────────────────────────────────────────────────
# MCP Server 連線設定
# ─────────────────────────────────────────────────────────────────────────────
# MCP Server WebSocket 位址（請替換為實際伺服器 IP）
MCP_SERVER_URL=ws://your-mcp-server-ip:30787

# 認證 Token（必須與 MCP Server 的 REMOTE_BROWSER_TOKEN 一致）
MCP_TOKEN=your-secret-token-here

# ─────────────────────────────────────────────────────────────────────────────
# Chrome CDP 設定
# ─────────────────────────────────────────────────────────────────────────────
# Chrome CDP Port（預設 9222）
CHROME_CDP_PORT=9222

# Chrome CDP Endpoint（通常不需要修改）
CHROME_CDP_ENDPOINT=http://localhost:9222

# Chrome User Data 目錄（Windows 預設使用 %TEMP%\chrome_remote_debug）
# CHROME_USER_DATA=/tmp/chrome_debug

# ─────────────────────────────────────────────────────────────────────────────
# Client 設定
# ─────────────────────────────────────────────────────────────────────────────
# Client ID（用於識別，可自訂）
CLIENT_ID=browser-agent

# ─────────────────────────────────────────────────────────────────────────────
# 連線設定
# ─────────────────────────────────────────────────────────────────────────────
# 重連間隔（秒）
RECONNECT_INTERVAL=5.0

# 心跳間隔（秒）
HEARTBEAT_INTERVAL=30.0

# 操作逾時（秒）
OPERATION_TIMEOUT=60.0